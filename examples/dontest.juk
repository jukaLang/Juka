sub main() = {
	foo();
	printLine("main");
	var bb = testClass();
}

sub process() = {
	printLine("process");
}

sub foo() = {
	process();
	printLine("foo");
}


class testClass = {
	sub one() = {
	}

	sub two() = {
	}
}